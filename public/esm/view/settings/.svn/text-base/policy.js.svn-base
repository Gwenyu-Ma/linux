define(function(require){
    var tpl = require('text!policy.html');
    var op = {
        init:function(container){
            $(container).append(tpl);
        }
    };
    return {
        render:function(container){
            op.init(container);
        }
    };
});